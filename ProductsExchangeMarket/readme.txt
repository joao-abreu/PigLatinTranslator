Uses html, css, JavaScript, knockoutJs;



Generates random prices for defined products, within a set price range for each product. The products are extracted randomly or sequentially in the time range (in minutes) that the user defines. By pressing “123” the user can manually raffle a new product.

